import { InteractiveImageAccordion } from "./interactive-image-accordion";

export default function DemoOne() {
  return (
    <div className="w-full bg-ink-raised overflow-hidden">
      <InteractiveImageAccordion items={[]} />
    </div>
  );
}
